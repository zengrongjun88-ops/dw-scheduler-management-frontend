# 独立HTML演示页面使用指南

## 文件说明

**文件**: `demo.html`
**用途**: 独立的单页面演示版本,无需后端服务和构建工具,可直接在浏览器中打开

## 功能特点

### ✅ 完全独立运行
- **零依赖**: 不需要npm install,不需要node_modules
- **零构建**: 不需要npm run build,不需要webpack/vite
- **零服务器**: 不需要后端API,不需要开发服务器
- **即开即用**: 双击HTML文件即可在浏览器中打开

### ✅ Mock数据完整
包含以下Mock数据:
- **任务管理**: 10条任务数据(SQL/Shell/Python类型)
- **数据源管理**: 10个数据源(MySQL/Hive/ClickHouse等)
- **实例管理**: 10条实例数据(不同状态)
- **服务器管理**: 5台服务器(Master/Worker)

### ✅ 核心功能
- ✅ 任务列表展示和搜索过滤
- ✅ 数据源列表展示
- ✅ 实例列表展示和筛选
- ✅ 服务器列表展示
- ✅ 分页功能
- ✅ 响应式布局

## 使用方法

### 方法1: 直接打开(推荐)

```bash
# Mac/Linux
open demo.html

# Windows
start demo.html

# 或直接双击 demo.html 文件
```

### 方法2: 通过浏览器打开

1. 打开浏览器(Chrome/Firefox/Safari/Edge)
2. 按 `Ctrl+O` (Windows) 或 `Cmd+O` (Mac)
3. 选择 `demo.html` 文件
4. 点击打开

### 方法3: 使用简单HTTP服务器(可选)

如果需要更接近真实部署环境:

```bash
# Python 3
python -m http.server 8000

# Node.js (需要全局安装http-server)
npx http-server

# 然后访问
open http://localhost:8000/demo.html
```

## 演示内容

### 1. 任务管理
- **任务列表**: 显示10条Mock任务
- **搜索功能**: 按任务名称搜索
- **类型筛选**: SQL/Shell/Python
- **状态筛选**: 启用/禁用/开发中
- **分页**: 每页10条
- **操作**: 编辑/查看/删除(UI展示)

### 2. 数据源管理
- **数据源列表**: 显示10个Mock数据源
- **类型展示**: MySQL/Hive/ClickHouse/Spark/PostgreSQL
- **连接信息**: JDBC URL完整展示
- **操作**: 测试连接/编辑/删除(UI展示)

### 3. 实例管理
- **实例列表**: 显示10条Mock实例
- **状态展示**: 成功/失败/运行中/等待中
- **搜索功能**: 按实例名称或任务名称搜索
- **状态筛选**: 按实例状态筛选
- **日期筛选**: 按业务日期筛选
- **分页**: 每页10条
- **操作**: 重跑/取消/查看日志/详情(UI展示)

### 4. 服务器管理
- **服务器列表**: 显示5台Mock服务器
- **角色展示**: Master/Worker
- **状态展示**: 在线/离线/备用
- **资源信息**: CPU核数、内存大小、任务负载
- **心跳监控**: 最后心跳时间

## 技术实现

### 架构特点
- **纯HTML+CSS+JavaScript**: 无任何框架依赖
- **原生DOM操作**: 高效的数据渲染
- **事件驱动**: 响应式交互
- **数据驱动UI**: Mock数据与UI分离

### 浏览器兼容性
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ⚠️ 不支持IE11

### 文件大小
- **HTML文件**: ~30KB
- **无外部依赖**: 0KB
- **总大小**: ~30KB

## 应用场景

### 1. 快速演示
```bash
# 无需任何准备,直接打开演示
open demo.html
# 适合客户演示、功能展示
```

### 2. 离线演示
- 将 `demo.html` 复制到U盘
- 在任何电脑上打开
- 无需网络连接
- 适合外出演示、展会展示

### 3. 培训教学
- 学员可以直接打开查看
- 无需安装任何工具
- 降低学习门槛
- 适合用户培训、新人引导

### 4. 功能验收
- 产品经理可以直接打开验收
- 无需配置开发环境
- 快速验证UI和交互
- 适合原型验证、需求确认

### 5. 文档附件
- 作为文档的可交互附件
- 比静态图片更直观
- 比视频更灵活
- 适合技术文档、产品文档

## 扩展开发

### 添加新的Mock数据

编辑 `demo.html` 中的 `mockData` 对象:

```javascript
const mockData = {
    tasks: [
        // 在这里添加更多任务数据
        {id: 11, taskName: 'new_task', ...}
    ],
    // ...
};
```

### 添加新功能

在 `<script>` 标签中添加新的JavaScript函数:

```javascript
function newFeature() {
    // 实现新功能
}
```

### 修改样式

在 `<style>` 标签中添加或修改CSS:

```css
.custom-style {
    /* 自定义样式 */
}
```

## 限制说明

### 功能限制
- ❌ 不支持真实的CRUD操作(操作按钮仅UI展示)
- ❌ 不支持数据持久化(刷新后数据重置)
- ❌ 不支持复杂的DAG图展示
- ❌ 不支持代码编辑器
- ❌ 不支持实时日志查看
- ❌ 不支持图表监控

### 数据限制
- Mock数据固定在HTML中
- 不支持动态加载更多数据
- 数据量较小(适合演示)

### 交互限制
- 按钮操作仅为UI展示
- 不支持表单提交
- 不支持弹窗编辑

## 对比完整版本

| 特性 | demo.html | 完整Mock版本 | 说明 |
|-----|----------|------------|------|
| **启动方式** | 双击打开 | npm run dev:mock | demo更简单 |
| **依赖要求** | 无 | Node.js + npm | demo零依赖 |
| **文件大小** | 30KB | ~2MB | demo更轻量 |
| **功能完整度** | 60% | 100% | 完整版更强大 |
| **交互能力** | 基础展示 | 完整CRUD | 完整版可交互 |
| **数据持久化** | 不支持 | localStorage | 完整版支持 |
| **代码编辑器** | 不支持 | Monaco Editor | 完整版支持 |
| **DAG图** | 不支持 | AntV G6 | 完整版支持 |
| **实时日志** | 不支持 | 支持 | 完整版支持 |
| **图表监控** | 不支持 | ECharts | 完整版支持 |

**使用建议**:
- **快速演示**: 使用 `demo.html`
- **功能验收**: 使用 `demo.html`
- **完整测试**: 使用完整Mock版本
- **开发调试**: 使用完整Mock版本

## 升级路径

如果需要完整功能,可以切换到完整Mock版本:

```bash
# 1. 切换到Mock分支
git checkout feature/mock-version

# 2. 启动完整Mock版本(需要解决npm权限问题)
npm run dev:mock

# 或使用预构建的静态文件
npm run build:mock
```

## 维护建议

### 更新Mock数据
定期更新Mock数据,保持与真实业务场景一致

### 同步功能更新
当完整版本增加新功能时,考虑是否需要在demo.html中简化实现

### 文档更新
保持本文档与demo.html的功能描述同步

## 故障排除

### 打开后显示空白
- 检查浏览器控制台是否有JavaScript错误
- 尝试使用Chrome浏览器打开
- 确保文件编码为UTF-8

### 样式显示异常
- 清除浏览器缓存
- 使用无痕模式打开
- 检查是否被浏览器扩展影响

### 功能不工作
- 确保JavaScript已启用
- 检查浏览器版本是否过旧
- 查看浏览器控制台错误信息

## 相关链接

| 资源 | 链接 |
|-----|------|
| 完整Mock版本 | feature/mock-version分支 |
| Mock实现计划 | claude/FRONTEND_MOCK_PLAN.md |
| Mock交付报告 | claude/MOCK_DELIVERY_REPORT.md |
| 前端仓库 | https://github.com/zengrongjun88-ops/dw-scheduler-management-frontend |

## 反馈与支持

如有问题或建议,请通过以下方式反馈:
- GitHub Issues
- 项目维护者

---

**版本**: V1.0
**创建日期**: 2026-01-22
**最后更新**: 2026-01-22
**维护人**: Claude
